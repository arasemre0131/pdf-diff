version: '3.8'

services:
  redis:
    image: redis:7.0-alpine
    container_name: pdf-api-redis
    ports:
      - "6379:6379"
    networks:
      - pdf-api-network

  minio:
    image: minio/minio:latest
    container_name: pdf-api-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-data:/minio_data
    command: minio server /minio_data --console-address ":9001"
    networks:
      - pdf-api-network

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: pdf-api-backend
    ports:
      - "8000:8000"
    environment:
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/0
      S3_BUCKET: pdf-uploads
      S3_ENDPOINT_URL: http://minio:9000
    depends_on:
      - redis
      - minio
    networks:
      - pdf-api-network

volumes:
  minio-data:

networks:
  pdf-api-network:
    driver: bridge
